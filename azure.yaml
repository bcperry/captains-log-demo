name: captains-log
services:
  api:
    project: .
    language: docker
    host: containerapp
    docker:
      buildArgs:
        - VITE_AZURE_CLIENT_ID=${AZURE_ENTRA_CLIENT_ID}
        - VITE_AZURE_TENANT_ID=${AZURE_ENTRA_TENANT_ID}
        - VITE_AZURE_CLOUD=${AZURE_CLOUD}